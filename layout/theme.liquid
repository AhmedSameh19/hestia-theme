<!doctype html>
<html lang="{{ request.locale.iso_code }}" class="scroll-smooth">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="theme-color" content="#F8F3EE">
    <link rel="canonical" href="{{ canonical_url }}">
    <link rel="icon" type="image/png" href="{{ settings.favicon | default: 'logo-monogram-cream-bg.png' | asset_url }}">

    <title>
      {{- page_title -}}
      {%- if current_page != 1 %} &ndash; {{ 'general.page' | t }} {{ current_page }}{% endif -%}
      {%- unless page_title contains shop.name %} | {{ shop.name }}{% endunless -%}
    </title>
    {% if page_description %}
      <meta name="description" content="{{ page_description | escape }}">
    {% endif %}

    {%- comment -%} Set dark class before paint to avoid a flash {%- endcomment -%}
    <script>
      (function () {
        var mode = localStorage.getItem('hestia:appearance') || '{{ settings.appearance | default: "light" }}';
        if (mode === 'dark' || (mode === 'system' && matchMedia('(prefers-color-scheme: dark)').matches)) {
          document.documentElement.classList.add('dark');
          document.querySelector('meta[name="theme-color"]').content = '#2D2D2D';
        }
      })();
    </script>

    {%- comment -%} ponytail: DM Sans + Material Symbols from Google CDN; self-host if the store must avoid third-party requests {%- endcomment -%}
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&display=swap" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200&display=swap" rel="stylesheet">

    {{ 'theme.css' | asset_url | stylesheet_tag }}
    <script>
      window.hestiaStrings = {
        addToCart: {{ 'products.add_to_cart' | t | json }},
        added: {{ 'products.added' | t | json }},
        soldOut: {{ 'products.sold_out' | t | json }},
        unavailable: {{ 'products.unavailable' | t | json }}
      };
    </script>
    <script src="{{ 'theme.js' | asset_url }}" defer></script>

    {{ content_for_header }}
  </head>

  <body class="bg-background text-on-surface font-body-md selection:bg-secondary-container">
    {% section 'header' %}

    <main id="MainContent" class="pt-20">
      {{ content_for_layout }}
    </main>

    {% section 'footer' %}
  </body>
</html>
